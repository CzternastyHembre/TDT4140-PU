/*
import BuyPost from ".../.../src/core/BuyPost";

//Valid fields to use for testing
const validUser = "";
const validEventName = "";
const validEventType = "";
const validDateAndTime = "";

describe("Test BuyPost class", () => {
  let testBuyPost;

  beforeEach(() => {
    testBuyPost = new BuyPost(
      validUser,
      validEventName,
      validEventType,
      validDateAndTime
    );
  });

  //User test
  const newValidUser = "";
  it("Test the valid user: " + newValidUser, () => {
    expect(() => testBuyPost.setUser(newValidUser)).not.toThrow();
  });

  const invalidUsers = ["", ""];
  invalidUsers.forEach((user) => {
    it("Test the invalid user: " + user, () => {
      expect(() => testBuyPost.setUser(user)).toThrow();
    });
  });

  //EventName test
  const newValidEventName = "";
  it("Test the valid event name: " + newValidEventName, () => {
    expect(() => testBuyPost.setEventName(newValidEventName)).not.toThrow();
  });

  const invalidEventNames = ["", ""];
  invalidEventNames.forEach((eventName) => {
    it("Test the invalid event name: " + eventName, () => {
      expect(() => testBuyPost.setEventName(eventName)).toThrow();
    });
  });

  //EventType test
  const newValidEventType = "";
  it("Test the valid event type: " + newValidEventType, () => {
    expect(() => testBuyPost.setEventType(newValidEventType)).not.toThrow();
  });

  const invalidEventTypes = ["", ""];
  invalidEventTypes.forEach((eventType) => {
    it("Test the invalid event type: " + eventType, () => {
      expect(() => testBuyPost.setEventType(eventType)).toThrow();
    });
  });

  //DateAndTime test
  const newValidDateAndTime = "";
  it("Test the valid date and time: " + newValidDateAndTime, () => {
    expect(() => testBuyPost.setDateAndTime(newValidDateAndTime)).not.toThrow();
  });

  const invalidDateAndTimes = ["", ""];
  invalidDateAndTimes.forEach((dateAndTime) => {
    it("Test the invalid date and time: " + dateAndTime, () => {
      expect(() => testBuyPost.setDateAndTime(dateAndTime)).toThrow();
    });
  });
});
*/
